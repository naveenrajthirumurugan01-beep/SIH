// TODO: replace with real widget tests once Firebase test setup
// (e.g. firebase_auth_mocks / fake_cloud_firestore) is added, since
// LandslideEwsApp requires Firebase.initializeApp() to have run.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
